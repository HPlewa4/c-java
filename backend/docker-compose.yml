services:
  backend:
    build: .
    ports:
      - "8080:8080"
    environment:
      - PORT=8080
      - MODELS_DIR=/app/models
    volumes:
      - ./models:/app/models
    restart: unless-stopped
